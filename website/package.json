{
  "private": true,
  "scripts": {
    "build": "pwa export",
    "start": "sirv build -s",
    "watch": "pwa watch"
  },
  "dependencies": {
    "ganalytics": "latest",
    "preact": "latest",
    "sirv-cli": "latest"
  },
  "devDependencies": {
    "@pwa/cli": "^0.3.2",
    "@pwa/plugin-sw-precache": "^0.3.1",
    "@pwa/preset-preact": "^0.1.0"
  }
}
