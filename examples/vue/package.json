{
  "scripts": {
    "build": "pwa build",
    "start": "sirv build -s",
    "watch": "pwa watch"
  },
  "dependencies": {
    "sirv-cli": "^0.1.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@pwa/cli": "^0.0.1-0",
    "@pwa/preset-vue": "^0.0.1-0",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
