{
  "scripts": {
    "build": "pwa export",
    "start": "sirv build -s",
    "watch": "pwa watch"
  },
  "dependencies": {
    "navaid": "^1.0.0",
    "sirv-cli": "^0.1.5"
  },
  "devDependencies": {
    "@pwa/cli": "^0.0.2",
    "@pwa/preset-svelte": "^0.0.2",
    "copy-webpack-plugin": "^4.5.2",
    "svelte": "^2.9.9"
  }
}
