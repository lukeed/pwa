{
  "scripts": {
    "build": "pwa build",
    "start": "sirv build -s",
    "watch": "pwa watch"
  },
  "dependencies": {
    "sirv-cli": "^0.1.5"
  },
  "devDependencies": {
    "@pwa/cli": "^0.0.1-0",
    "@pwa/preset-svelte": "^0.0.1-0",
    "copy-webpack-plugin": "^4.5.2",
    "svelte": "^2.9.1"
  }
}
